- specific_title [t('shared.controls.new'), t('shared.titles.election')]
- add_breadcrumb "#{t('shared.titles.election')}: #{t('shared.controls.new')}", request.path
- add_breadcrumb t('shared.titles.elections'), :elections_path
- content_for :menu do
  = render 'shared/menu'

.container
  %h1= "#{t('shared.controls.new')}: #{t('shared.titles.election')}"
  %p.lead= t('.instructions')

  = render partial: 'form', locals: { election: @election }
