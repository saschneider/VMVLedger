-#  Link to the "First" page
-#  available local variables
-#    url:           url to the first page
-#    current_page:  a page object for the currently displayed page
-#    total_pages:   total number of pages
-#    per_page:      number of items to fetch per page
-#    remote:        data-remote
%li.page-item{ class: current_page.first? ? 'disabled' : '' }
  = link_to url, remote: remote, class: 'page-link', aria: { label: t('shared.controls.first') } do
    %span{ aria: { hidden: true } }= fa_icon "angle-double-left"
    %span.sr-only= t('shared.controls.first')
