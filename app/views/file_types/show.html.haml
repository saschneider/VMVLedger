- specific_title [@file_type.name, @file_type.name]
- add_breadcrumb @file_type.name, request.path
- add_breadcrumb t('shared.titles.file_types'), :file_types_path
- content_for :menu do
  = render 'shared/menu'

.container
  %h1= @file_type.name
  %p.lead= t('.instructions')

  = render partial: 'file_type', locals: { file_type: @file_type }
