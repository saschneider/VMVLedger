- specific_title [t('shared.controls.new'), t('shared.titles.file_type')]
- add_breadcrumb "#{t('shared.titles.file_type')}: #{t('shared.controls.new')}", request.path
- add_breadcrumb t('shared.titles.file_types'), :file_types_path
- content_for :menu do
  = render 'shared/menu'

.container
  %h1= "#{t('shared.controls.new')}: #{t('shared.titles.file_type')}"
  %p.lead= t('.instructions')

  = render partial: 'form', locals: { file_type: @file_type }
