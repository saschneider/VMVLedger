- specific_title [@upload.file_type.name, t('shared.titles.upload')]
- add_breadcrumb @upload.file_type.name, request.path
- add_breadcrumb t('shared.titles.uploads'), :uploads_path
- content_for :menu do
  = render 'shared/menu'

.container
  %h1= @upload.file_type.name
  %p.lead= t('.instructions')

  = render partial: 'upload', locals: { upload: @upload }
