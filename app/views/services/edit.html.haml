- specific_title [t('shared.controls.edit'), t('shared.titles.service')]
- add_breadcrumb "#{t('shared.controls.edit')}: #{t('shared.titles.service')}", request.path
- content_for :menu do
  = render 'shared/menu'

.container
  %h1= "#{t('shared.controls.edit')}: #{t('shared.titles.service')}"
  %p.lead= t('.instructions')

  = render partial: 'form', locals: { service: @service }
