- specific_title [t('shared.controls.edit'), t('shared.titles.user')]
- add_breadcrumb "#{@user.fullname}: #{t('shared.controls.edit')}", request.path
- add_breadcrumb t('shared.titles.users'), :users_path
- content_for :menu do
  = render 'shared/menu'

.container
  %h1= "#{t('shared.controls.edit')}: #{t('shared.titles.user')}"
  %p.lead= t('.instructions')

  = render partial: 'form', locals: { user: @user }
