- specific_title [t('shared.titles.forgotten_password')]
- add_breadcrumb t('shared.titles.forgotten_password'), request.path
- content_for :menu do
  = render 'shared/menu'

.container
  %h1= t('shared.titles.forgotten_password')
  %p.lead= t('.instructions')

  = bootstrap_form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }, layout: :horizontal, label_col: 'col-sm-3', control_col: 'col-sm-9') do |form|
    = form_field(form, :email_field, :email, control_col: 'col-sm-5', autofocus: true)

    = render partial: 'shared/form_mandatory', locals: { form: form }
    = form.form_group do
      = form.submit t('shared.controls.send')